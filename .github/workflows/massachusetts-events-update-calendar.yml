name: Update Massachusetts events calendars

on:
  workflow_dispatch:
  schedule:
    - cron: "47 11 * * *"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 python-dateutil icalendar playwright
          python -m playwright install --with-deps chromium

      - name: Generate Massachusetts calendar
        run: python generate_calendar.py

      - name: Generate Worcester Colleges calendar
        run: python generate_worcester_colleges_feed.py

      - name: Generate Central Massachusetts calendar
        run: python generate_central_massachusetts_feed.py

      - name: Generate Northborough calendar
        run: python generate_northborough.py

      - name: Generate Events Near Me calendar
        run: python generate_events_near_me.py

      - name: Commit updated calendars
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add massachusetts-events.ics central-massachusetts-events.ics worcester-colleges-events.ics northborough-events.ics events-near-me.ics
          if git diff --cached --quiet; then
            echo "No calendar changes"
          else
            git commit -m "Update separate Massachusetts event calendar feeds"
            git push
          fi
